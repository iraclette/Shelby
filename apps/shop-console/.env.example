SUPABASE_URL=
SUPABASE_ANON_KEY=

# Optional. Folder the product photo picker opens by default — point this at
# wherever your phone's photo sync (iCloud for Windows, Phone Link, etc.)
# downloads new photos, so newly taken product photos are one click away.
# Leave blank to let Shop Console try a few common sync folders automatically.
PHOTOS_SYNC_DIR=
